import React from 'react';
import {
  HeaderBar,
  Title,
} from './Header.styles';

const Header = props => (
  <HeaderBar>
    <Title>
        Brokk and Odin
    </Title>
  </HeaderBar>
);

export default Header;
